% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{addin_chat_panel}
\alias{addin_chat_panel}
\alias{chat_gadget}
\title{Open the LLMcoder Chat Panel}
\usage{
addin_chat_panel(system_prompt_override = NULL)

chat_gadget(system_prompt_override = NULL)
}
\arguments{
\item{system_prompt_override}{Override the auto-generated system prompt.
Use with caution; this replaces the default persona entirely.
Pass \code{NULL} (the default) to use the built-in prompt templates.}
}
\value{
Invisible \code{NULL} (launches a Shiny gadget and does not
return until the gadget is closed).
}
\description{
Launches an interactive multi-turn chat powered by your configured LLM
directly inside RStudio.  The panel supports:
\itemize{
\item Multi-turn conversation history.
\item Session Context awareness (loaded packages, global objects,
source editor contents, console history).
\item One-click code execution: each code block (marked
\verb{```r}) includes a
\strong{Run} button that sends the code to the R console.
\item Prompt style presets: General, R Code Helper, Statistics Advisor,
Research (Psycho).
\item Session context toggle.
\item Transcript export.
}
}
\details{
Bind a keyboard shortcut in RStudio (\strong{Tools -> Modify Keyboard
Shortcuts...}) to \code{addin_chat_panel()} for quick access
(\kbd{Ctrl+Shift+L} is a common choice).
}
\examples{
\dontrun{
# Open the chat panel (assigns Ctrl+Shift+L in RStudio shortcuts)
addin_chat_panel()

# Start with a custom system prompt
addin_chat_panel(system_prompt_override = "You are a strict R reviewer.")
}

}
\seealso{
\code{\link[=llmcoder_setup]{llmcoder_setup()}}, \code{\link[=addin_generate_from_comment]{addin_generate_from_comment()}}
}
\keyword{internal}
